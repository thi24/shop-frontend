version: '3'

services:

  shop-frontend:
    image: ghcr.io/thi24/shop-frontend:latest
    build:
      context: .
      dockerfile: dockerfile
    networks:
      - nginx-proxy-manager

networks:
  nginx-proxy-manager:
    external: true
