version: '3.8'

services:

  shop-frontend:
    image: ghcr.io/thi24/shop-frontend:latest
    build:
      context: .
      dockerfile: Dockerfile
    networks:
      - nginx-proxy-manager

networks:
  nginx-proxy-manager:
    external: true